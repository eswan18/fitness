{
  // Rewrite all routes to index.html to enable client-side routing (React Router)
  // This ensures routes like /oauth/callback are handled by the React app
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
